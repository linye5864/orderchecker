# OrderComparer 核心对账流程验收测试文档

## 测试环境准备

### 1. 启动后端服务

```bash
cd packages/backend
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload
```

**验收标准**: 终端显示 `Application startup complete.`

### 2. 启动前端服务

```bash
cd packages/renderer
npm run dev
```

**验收标准**: 浏览器可访问 `http://localhost:5173`

---

## 测试场景一：文件上传与识别

### 操作步骤

1. 登录系统（默认账号: admin / admin）
2. 进入【数据上传】页面
3. 依次上传三个文件：
   - 配送单.xlsx
   - 流水账单.xlsx
   - 闪送账单.xlsx (或其他平台账单)

### 验收标准

- [ ] 每个文件上传后显示 **"✓ 已就绪"** 绿色标签
- [ ] 文件类型正确识别（配送单=delivery, 流水=flow, 平台=platform）
- [ ] 文件大小正确显示（非 0 KB）
- [ ] 【进入对账控制台】按钮变为可点击状态（蓝色）

---

## 测试场景二：智能预检流程

### 操作步骤

1. 点击【进入对账控制台】
2. 在步骤1"数据映射"页面，确认三个文件均显示"✓ 已就绪"
3. 点击【下一步：智能预检】
4. 等待预检自动执行（约2秒）

### 验收标准 - 预检项目

- [ ] **文件格式识别**: 状态=通过，显示"格式正确"
- [ ] **数据完整性校验**: 状态=通过，显示文件大小（如"文件大小适中（XXX KB）"）
  - ⚠️ **关键修复点**: 不再显示"文件过小"错误
- [ ] **订单周期匹配**: 状态=通过/警告（取决于文件名是否包含日期）
- [ ] **数据量级分析**: 状态=通过，显示估算订单数

### 验收标准 - 汇总状态

- [ ] 顶部显示"X项通过"（至少2项）
- [ ] 如有警告，显示黄色"⚠ 警告"卡片
- [ ] 【开始对账】按钮变为可点击状态

---

## 测试场景三：核心对账执行

### 操作步骤

1. 在预检通过后，点击【开始对账】
2. 观察进度条和日志输出
3. 等待对账完成（时间取决于数据量）

### 验收标准 - 进度反馈

- [ ] 进度条从 5% 平滑增长到 100%
- [ ] 日志实时显示关键步骤：
  ```
  ✅ 启动对账任务...
  ℹ️ 配送单: XXX.xlsx
  ℹ️ 流水账单: XXX.xlsx
  ✅ 任务已创建: xxxxxxxx...
  ℹ️ 计算完成，生成报告中...
  ✅ 对账完成!
  ```

### 验收标准 - 完成状态

- [ ] 进度条显示 100%
- [ ] 日志显示对账统计：
  - 总订单数
  - 匹配成功数
  - 异常订单数
  - 匹配率
- [ ] 自动跳转到步骤4"生成报告"

---

## 测试场景四：对账结果查看

### 操作步骤

1. 在步骤4页面，点击【查看详细报告】
2. 进入"核心审计看板"页面
3. 切换不同的页签查看数据

### 验收标准 - 顶部指标磁贴

- [ ] 显示5个核心指标：
  - 有效单量(去重)
  - 配送应扣(预估)
  - 流水实扣(账单)
  - 三方实收(成本)
  - 财务对齐率

### 验收标准 - 【逐单穿透】页签

- [ ] 表格显示订单明细，包含：
  - 订单号
  - 系统应扣
  - 流水实扣
  - 平台成本
  - 毛损益/差异
  - 审计意见
- [ ] 点击订单行，弹出详情弹窗
- [ ] 弹窗显示三方金额对比和审计差异

### 验收标准 - 【商户追踪】页签

- [ ] 显示商户列表，包含：
  - 商户ID (admin_id)
  - 有效单量
  - 累计支出
  - 充值/获赠
  - 余额状态 (期初 → 期末)
  - 审计状态 (🟢 资金平衡 / 🔴 账目偏差)
- [ ] 点击商户行，弹出该商户的交易明细

### 验收标准 - 【操作异常分析】页签

- [ ] 如有异常订单，显示在列表中
- [ ] 异常特征识别准确（如"已取消仍扣费"）
- [ ] 如无异常，显示"未发现操作异常订单，系统稳定性良好 ✅"

---

## 关键修复点验证

### 修复1: 文件识别兼容性

**问题**: 上传后文件类型不匹配，导致预检找不到文件
**修复**: 扩展识别逻辑，支持 `dispatch/delivery/LOCAL` 等多种标识
**验证**:

- [ ] 上传的配送单能被正确识别为 delivery
- [ ] 上传的流水账单能被正确识别为 flow
- [ ] 上传的平台账单能被正确识别为 platform

### 修复2: 文件大小获取

**问题**: 文件上传后 size 字段为 0，导致预检误判"文件过小"
**修复**:

- 后端返回 `file_size_bytes` 字段
- 前端正确解析并设置 `size` 属性
  **验证**:
- [ ] 上传后文件列表显示正确的文件大小（非 0 KB）
- [ ] 预检"数据完整性校验"通过，不再报"文件过小"错误

### 修复3: 多格式文件支持

**问题**: 后端仅支持 .xlsx，不支持 .xls 和 .csv
**修复**: 添加 `_read_data_file` 通用读取方法
**验证**:

- [ ] 可以上传 .xlsx 文件
- [ ] 可以上传 .xls 文件（旧版Excel）
- [ ] 可以上传 .csv 文件

---

## 性能验收标准

### 小数据集 (< 1000 单)

- [ ] 预检完成时间 < 3秒
- [ ] 对账完成时间 < 10秒

### 中等数据集 (1000-5000 单)

- [ ] 预检完成时间 < 5秒
- [ ] 对账完成时间 < 30秒

### 大数据集 (> 5000 单)

- [ ] 预检完成时间 < 10秒
- [ ] 对账完成时间 < 60秒
- [ ] 进度条平滑更新，无卡顿

---

## 异常场景测试

### 场景1: 上传空文件

**操作**: 上传一个空的 .xlsx 文件
**预期**:

- [ ] 上传成功，但预检时"数据完整性校验"失败
- [ ] 显示"文件过小"警告

### 场景2: 上传错误格式

**操作**: 上传一个 .pdf 或 .txt 文件
**预期**:

- [ ] 上传被拒绝，或上传后预检"文件格式识别"失败

### 场景3: 缺少必选文件

**操作**: 只上传配送单和流水账单，不上传平台账单
**预期**:

- [ ] 可以进入对账控制台
- [ ] 预检通过
- [ ] 对账执行时自动降级为"二方对账"（配送单 vs 流水单）

### 场景4: 网络中断

**操作**: 对账执行到一半时断开网络
**预期**:

- [ ] 前端显示错误提示
- [ ] 可以重新执行对账

---

## 回归测试检查清单

- [ ] 登录/登出功能正常
- [ ] 文件上传进度条显示正常
- [ ] 文件删除功能正常
- [ ] 历史任务列表可查看
- [ ] 导出功能可用（如有）
- [ ] 多用户并发上传不冲突
- [ ] 浏览器刷新后状态保持（Token 持久化）

---

## 已知问题与限制

1. **文件大小限制**: 单文件最大 50MB
2. **并发限制**: 建议同时运行的对账任务不超过 3 个
3. **浏览器兼容性**: 推荐使用 Chrome/Edge 最新版本
4. **数据保留**: 对账结果默认保留 30 天

---

## 测试完成标准

✅ **通过标准**: 上述所有验收标准至少 90% 通过
⚠️ **需优化**: 通过率 70%-90%，需记录问题并排期修复
❌ **不通过**: 通过率 < 70%，需立即修复核心问题

---

## 测试报告模板

```
测试日期: YYYY-MM-DD
测试人员: XXX
测试环境: Windows/Mac/Linux + Chrome/Edge/Firefox
数据集: 配送单 XXX 行，流水账单 XXX 行，平台账单 XXX 行

场景一: ✅ 通过 / ⚠️ 部分通过 / ❌ 失败
场景二: ✅ 通过 / ⚠️ 部分通过 / ❌ 失败
场景三: ✅ 通过 / ⚠️ 部分通过 / ❌ 失败
场景四: ✅ 通过 / ⚠️ 部分通过 / ❌ 失败

关键问题:
1. XXX
2. XXX

建议:
1. XXX
2. XXX
```

---

**最后更新**: 2026-01-24
**版本**: V2.0 (三方对账引擎)
